/* Root page — will host the full client shell once components are built */
export default function Home() {
  return (
    <div style={{ padding: "40px 56px" }}>
      <h1 style={{ fontFamily: "'Cormorant Garamond', Georgia, serif", fontSize: 56, fontStyle: "italic", fontWeight: 700, color: "#221516", lineHeight: 0.9 }}>
        StyleCipher
      </h1>
      <p style={{ fontFamily: "'Jost', sans-serif", fontWeight: 300, marginTop: 16, color: "#6A5A4A", fontSize: 13 }}>
        Scaffold complete — components coming next.
      </p>
    </div>
  );
}
